---
id: 3-12-working-with-props
title: 3.12 Working With Props
date: 2021-03-12 16:09:15
---

import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

Props is simply an object that gives us acces to all the attributes we pass to our own React component.

There is one slight difference in the two ways you define React components:

> Added `this` in `class components`

<Tabs defaultValue="class" values={[
{ label: "Class", value: "class" },
{ label: "Function", value: "function" }
]}>
<TabItem value="class">

```jsx
class Person extends React.Component {
  render() {
    return (
      <p>
        I'm {this.props.name}
      </p>
    )
  }
}
```

</TabItem>

<TabItem value="function">

```jsx
const person = (props) => {
  return (
    <p>
      I'm {props.name}
    </p>
  )
}
```

</TabItem>
</Tabs>
